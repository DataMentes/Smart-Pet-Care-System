name: smart_pet_care
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0' # تم تحديث إصدار الـ SDK ليتوافق مع أحدث المكتبات

dependencies:
  flutter:
    sdk: flutter

  # --- المكتبات التي أضفناها ---
  # Core & Backend
  http: ^1.2.1
  shared_preferences: ^2.2.3

  # Firebase
  firebase_core: ^2.32.0
  firebase_messaging: ^14.9.3

  # UI & Widgets
  pinput: ^4.0.0
  fl_chart: ^0.68.0
  
  # Default Icon Pack
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true